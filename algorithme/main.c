#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;
    nombre = int(input("Entrez un nombre :"));
    for i in range (1,11):
            resultat = nomber*i
    printf(nombre,"x",i,"=", resultat);

    return 0;
}
